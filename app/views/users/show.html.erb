<div class="container">
  <%= notice %>
  <table class="table">
    <tbody>
    <tr>
      <th>名前</th>
      <td><%= @user.name%></td>
    </tr>
    <tr>
      <th>メールアドレス</th>
      <td><%= @user.email%></td>
    </tr>
    <tr>
      <th>作成日時</th>
      <td><%= @user.created_at%></td>
    </tr>
    </tbody>
  </table>
  <%= link_to "ログイン画面へ", new_session_path, class: 'btn btn-info '%>
</div>
